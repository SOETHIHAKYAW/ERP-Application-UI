import React from 'react';
import './Header.css'; // Ensure you have CSS for header

const Header = () => {
  return (
    <header className="header">
      <h1>ERP Application</h1>
    </header>
  );
};

export default Header;
